# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
CHEAP_MODEL=gpt-4o-mini  # Cheaper model for high-volume tasks (sentiment, news classification, etc.)

# Data Source API Keys (optional but recommended)
FINNHUB_API_KEY=your_finnhub_key_here
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key_here
FMP_API_KEY=your_fmp_key_here

# Portfolio settings
PORTFOLIO_HORIZON_END=2026-05-15
CANDIDATE_COUNT=60
# Optional constraints

# Email settings (for daily reports)
EMAIL_TO=romanp@tamu.edu
EMAIL_FROM=romanp@tamu.edu  # Optional, defaults to EMAIL_TO (TAMU SSO Gmail works fine)
SMTP_PASSWORD=your-gmail-app-password  # Generate from Google Account > Security > App Passwords
# SMTP_SERVER=smtp.gmail.com  # Optional, defaults to Gmail (works with TAMU SSO Gmail)
# SMTP_PORT=587  # Optional, defaults to 587

# MAYS AI submission
MAYS_PASSWORD=your-mays-password

# Backtest mode (optional)
# BACKTEST_MODE=true
# BACKTEST_DATE=2024-07-15  # Point-in-time date for backtesting
# BACKTEST_MODEL_CUTOFF=2024-04-01  # Model training cutoff to simulate
SECTOR_CAP=0.25
INDUSTRY_CAP=0.15
MIN_WEIGHT=0.02
MAX_WEIGHT=0.10
MIN_AVG_DOLLAR_VOLUME=5000000
BETA_MIN=0.5
BETA_MAX=1.5
